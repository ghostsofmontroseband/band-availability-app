import React from 'react';
import ReactDOM from 'react-dom/client';
import AvailabilityGrid from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AvailabilityGrid />
  </React.StrictMode>
);
